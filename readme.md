# Linux WSL automatic setup repository

This repository contains scripts that automatize the setup of a clean arch linux installation on WSL 2.

To do so, you just need to execute:

```bash
bash setup.sh
```
